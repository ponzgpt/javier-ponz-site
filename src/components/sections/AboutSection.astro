---
// Identity and the principle only. Skills, credentials and the chronology
// moved out to their own sections when the page was reordered.
import Page from '../Page.astro';
import { pages } from '../../data/pages.mjs';

const { lang = 'en' } = Astro.props;
const p = pages.about[lang];
---
<Page p={p} id="about" />
